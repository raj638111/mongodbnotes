{
  "name": "users",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nodemon --exec 'mocha -R min'",
    "_comment": "mocha is replaced by nodemon as it automates testing rather we manually running the test everytime there is a file change 6_30",
    "_comment": "Instead of nodemon, mocha's builtin watcher can also be used. But that has little bit of compatibility issues with mongoose 6_30 "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mocha": "^5.0.5",
    "mongoose": "^5.0.12",
    "nodemon": "^1.17.3"
  }
}
